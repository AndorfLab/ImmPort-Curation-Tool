# 🤔 Frequently Asked Questions 🧐


## 🧾 Where does the data that propagates the *Assessments* template come from?


| **Template Column**              | **Source of Value** |
|----------------------------------|----------------------|
| **Subject ID**                   | “Mappings” column (specified as `User Defined ID`) in the data dictionary |
| **Assessment Panel ID**          | Unique value generated by the tool |
| **Study ID**                     | "STUDY_ACCESSION" column from the uploaded ImmPort file (study_file.txt) |
| **Name Reported**                | "DESCRIPTION" column from the uploaded ImmPort file (study_file.txt) |
| **Assessment Type**              | User specified within the curation tool ("Assessment Name" column) |
| **Status**                       | NA |
| **CRF File Names**               | Study file name |
| **User Defined ID**              | Unique value generated by the tool |
| **Planned Visit ID**             | “Mappings” column (specified as `Visit`) in the data dictionary **AND** uploaded ImmPort file (planned_visit.txt) |
| **Name Reported**                | "Field Description" column in the data dictionary |
| **Study Day**                    | “Mappings” column (specified as `Study Day`) in the data dictionary **OR** "Override Study Day" column in the data dictionary |
| **Age at Onset Reported**        | "Age at Onset Reported" column in the data dictionary |
| **Age at Onset Unit Reported**   | "Age at Onset Unit Reported" column in the data dictionary |
| **Is Clinically Significant**    | NA |
| **Location of Finding Reported** | NA |
| **Organ or Body System Reported**| "Location" column in the data dictionary |
| **Result Value Reported**        | Study file (any column **not** specified in the “Mappings” column as `User Defined ID`, `Study Day`, `Study Time`, `Visit`, `Category`, or `NA`) |
| **Result Unit Reported**         | "Unit" column in the data dictionary |
| **Result Value Category**        | NA |
| **Subject Position Reported**    | NA |
| **Time of Day**                  | “Mappings” column (specified as `Study Time`) in the data dictionary **OR** "Override Study Time" column in the data dictionary |
| **Verbatim Question**            | "Verbatim Question" column in the data dictionary |
| **Who is Assessed**              | "Who is Assessed" column in the data dictionary |

## 🧪 Where does the data that propagates the *Lab Tests* template come from?



| **Template Column**              | **Source of Value** |
|----------------------------------|----------------------|
| **Biosample ID**                 | Unique value generated by the tool |
| **Lab Test Panel ID**            | Unique value generated by the tool |
| **Study ID**                     | "STUDY_ACCESSION" column from the uploaded ImmPort file (study_file.txt) |
| **Protocol ID(s)**               | "PROTOCOL_ACCESSION" column from the uploaded ImmPort file (protocol.txt) |
| **Subject ID**                   | “Mappings” column (specified as `User Defined ID`) in the data dictionary |
| **Planned Visit ID**             | “Mappings” column (specified as `Visit`) in the data dictionary **AND** uploaded ImmPort file (planned_visit.txt) |
| **Type**                         | User specified within the curation tool ("Type" column) |
| **Subtype**                      | User specified when "Type" = "Other" within the curation tool ("Subtype" column) |
| **Name**                         | NA |
| **Description**                  | NA |
| **Study Time Collected**         | “Mappings” column (specified as `Study Day`) in the data dictionary **OR** "Override Study Day" column in the data dictionary |
| **Study Time Collected Unit**    | "Unit" column in the data dictionary |
| **Study Time T0 Event**          | User specified within the curation tool ("Study Time T0 Event" column) |
| **Study Time T0 Event Specify**  | User specified when "Study Time T0 Event" = "Other" within the curation tool ("Study Time T0 Event Specify" column) |
| **Name Reported**                | User specified within the curation tool ("Name Reported" column) |
| **User Defined ID**              | Unique value generated by tool |
| **Name Reported1️⃣**             | "Field Description" column in the data dictionary |
| **Result Value Reported**        | Study file (any column **not** specified in the “Mappings” column as `User Defined ID`, `Study Day`, `Study Time`, `Visit`, `Category`, or `NA`) |
| **Result Unit Reported**         | "Unit" column in the data dictionary |

1️⃣: There are two “Name Reported” columns in the *Lab Tests* template. The first “Name Reported” column should describe the specific *Lab Test*, while the second “Name Reported” column should describe the “Result Value Reported”.


## ❓For the data dictionary, what is the "Verbatim Question" and how should it be filled in?

The verbatim question is designed to provide more complete context to a field than just a 1 or 2 word name. There are several cases where the verbatim question should be expanded beyond the question that is specifically listed for that field. 

**Compound Questions** 

Many times there are compound questions on a data collection form. For instance, there might be a question "Has the patient been treated with steroids before?" with a follow up question of "How many times?". The question "How many times?" loses context outside of the physical collection form. As such, the leading question of "Has the patient been treated with steroids before?" should be prepended to "How many times?" for the verbatim question. 

**Section Headers**

Sometimes a block of questions will have a section header that gives context to all the questions in the block. For instance, the header might say "What treatments were effective?". This would be followed by several brief statements, such as "Oral Steroids?", "Topical Steroids?", and "Food Elimination?". In this case, the text "What treatments were effective?" should be prepended to "Oral steroids?" and so forth.  

**Matrix/Table Fields**

Other times questions are arranged in a matrix or table display to aid in the compactness and usability of the collection form. In these cases, the rows and columns normally contain headers that are important. For instance, there might be a table similar to the following:

Does the participant have food allergies?
<table>
<tr>
    <th></th><th>Skin Prick Test</th><th>Patch Test</th><th>Rash/Hives</th><th>Difficulty Breathing</th><th>Anaphylaxis</th>
</tr>
<tr>
    <th>Eggs</th><td>0=Negative<br>1=Positive</td><td>0=Negative<br>1=Positive</td><td><input type='checkbox'></td><td><input type='checkbox'></td><td><input type='checkbox'></td>
</tr>
<tr>
    <th>Peanuts</th><td>0=Negative<br>1=Positive</td><td>0=Negative<br>1=Positive</td><td><input type='checkbox'></td><td><input type='checkbox'></td><td><input type='checkbox'></td>
</tr>
<tr>
    <th>Wheat</th><td>0=Negative<br>1=Positive</td><td>0=Negative<br>1=Positive</td><td><input type='checkbox'></td><td><input type='checkbox'></td><td><input type='checkbox'></td>
</tr>
<tr>
    <th>Soy</th><td>0=Negative<br>1=Positive</td><td>0=Negative<br>1=Positive</td><td><input type='checkbox'></td><td><input type='checkbox'></td><td><input type='checkbox'></td>
</tr>
</table>

In this case, each cell of the body of the table is a separate question. The row and column headers help define the question, along with the sentence above the table.

A good verbatim question for the first cell could be "Does the participant have food allergies? Eggs - Skin Prick Test". This includes the full context — the subject of the question, the specific condition being assessed, and the method of assessment — making it clear and interpretable when the data is reviewed or reused.



## 📅 For the data dictionary, what are the differences between specifying the `Study Day` value in "Column Mappings", using the "Override Study Day" column, and using the "Age at Onset Reported" column?


The study day that reflects each field in the study file is represented in the "Study Day" column of the *Assessments* template and the "Study Time Collected" column of the *Lab Tests* template. For most fields, the "Study Day" or "Study Time Collected" should be the day of the study visit.

In many cases, multiple fields will have the same study day or the entire case report form has the same study day. In these cases, the study day field should be specified in the "Mappings" column of the data dictionary as `Study Day`.

`Study Day` can only be specified once in "Mappings" per study file. However, in some cases, different fields in the same study file have differing study days. In these cases, the "Override Study Day" column from the data dictionary should be used for each field with a differing study day than the one defined in the "Mappings" column. 

For example, there might be a question "Have you had a headache since your last visit?" in the HEADACHE field. This could be accompanied by a follow-up question "What was the date of your last headache?" in the HEADACHE_DT field. This date (in integer form) can be placed in the "Study Day" column of the *Assessments* template by specifying HEADACHE_DT in the "Override Study Day" column of the data dictionary for the HEADACHE field.

The *Assessment* template also has an additional field that can contain a date —  "Age at Onset Reported". This should reflect the age that corresponds to a field. For instance, suppose the question "Do you have type II diabetes?" corresponds to the field DIABETES. The next field,  DIABETES_DT, contains the question  "At what age did you get type II diabetes?". In this case, you would designate DIABETES_DT in the "Age at Onset Reported" column of the data dictionary. 

## 📂 For the curation tool, where are the outputted files?

If the files were successfully generated, they will be saved in a *results* folder located in the parent directory of the study files directory that was loaded in the “3. Study File” tab.

For example, if the study files directory was `documents/vaccine-study/studyfile`, then the results will be located at `documents/vaccine-study/results`.

Inside the 'results' folder, there will be a subfolder named with the current date. The generated templates in TXT format will be inside that dated folder.

Note: Recreating a template on the same date overrides previous templates there were generated that day. Templates generated on different dates are stored in separate dated folders and will not override each other. 

## ✂️ For the outputted templates, what does [CUT] mean? 

If there is a [CUT] at the end of an entry, it indicates that the text exceeded the maximum character limit defined by ImmPort. The tool automatically truncates the text and appends [CUT] to signal that the original content was longer than allowed.










